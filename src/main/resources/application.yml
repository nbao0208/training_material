spring:
  data:
    mongodb:
      uri: ${MONGODB_URL:mongodb://mongo_user:m0ng0p4ssw0rd@localhost:27017/?authSource=admin}
      database: ${MONGODB_DATABASE:dev}
      auto-index-creation: true
  datasource:
    driver-class-name: org.postgresql.Driver
    password: baoisamazing
    url: jdbc:postgresql://localhost:5433/training_material
    username: baonguyen
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true

server:
    servlet:
      encoding:
        charset: UTF-8
