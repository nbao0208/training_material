spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 123
    url: jdbc:postgresql://localhost:5433/training_material
    username: baonguyen
  data:
    mongodb:
      uri: mongodb://baonguyen:123@localhost:27017
      authentication-database: admin
      database: training_material_mongodb

    redis:
      host: localhost
      port: 6379
      timeout: 300 #time the redis wait for the action on it
      password: 123
      url: redis://123@localhost:6379
      lettuce:
        pool:
          max-active: 8
          max-idle: 9
          min-idle: 1

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true



server:
    servlet:
      encoding:
        charset: UTF-8
